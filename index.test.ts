import { Coalesce } from "./index";

describe("coalesce-typescript-sdk", () => {
    it("initialize client", async () => {
        const coalesce = new Coalesce({
            apiKey: "API_KEY",
        });
    });
});
